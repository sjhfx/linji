# 仓库指南

## 项目结构与模块组织

本仓库是一个 GitBook 风格的个人数据分析知识库。根目录文件定义书籍入口：`README.md` 是首页，`SUMMARY.md` 控制目录导航，`book.json` 保存 GitBook 配置，`refer.md` 存放参考资料。主要内容位于：

- `model/`：分析模型与思维模型，按阶段分组。
- `system/`：数据化分析系统的指南、常见问题、用户反馈和更新日志。
- `tech/`：时间、情绪、选择、习惯等主题页面。
- `static/` 和 `.gitbook/assets/`：Markdown 页面引用的图片资源。
- `data/`：辅助数据集，例如 Excel 文件。

新增页面时，先放入对应内容目录，并在同一次修改中更新 `SUMMARY.md`。

## 构建、测试与开发命令

- `gitbook serve`：本地预览书籍，前提是已安装 GitBook CLI。
- `gitbook build`：生成用于发布的静态书籍文件。
- `git status`：提交前检查变更文件。
- `git diff -- SUMMARY.md path/to/page.md`：同时检查目录导航和页面内容变更。

本仓库没有应用构建流程或自动化测试套件。验证重点是 Markdown 渲染、链接可访问、图片路径正确，以及本地 GitBook 预览效果。

## 写作风格与命名约定

内容页面使用 Markdown。标题层级要清晰，每个页面只保留一个 `#` 一级标题。现有模型文件采用编号加大写缩写的命名方式，例如 `001FGXW.md` 和 `084SMART.md`；新增模型条目也应沿用该模式。资源路径应保持相对、稳定；移动图片前必须同步更新所有引用。

## 验证指南

提交前，确认新增或修改过的 Markdown 能正常渲染，内部链接可跳转，图片能显示。对于 `data/` 中的数据文件，需要在相关 Markdown 页面中补充简短说明，说明该文件支撑什么内容。

## 提交与 Pull Request 规范

近期提交记录使用简短直接的提交信息，并常用 `A`、`U`、`D` 分别表示新增、更新和删除。提交信息应简洁说明修改意图，例如：`U update system FAQ` 或 `A add RFM model note`。

Pull Request 应说明修改了哪些内容，列出 `SUMMARY.md` 的导航调整；如果涉及版式、图片或 GitBook 渲染变化，应附截图。

## Agent 专用说明

优先保证内容准确、读者易懂，不要为了结构精巧而增加维护成本。删除或重命名现有页面、图片、数据文件前，必须先检查所有依赖它们的引用。
